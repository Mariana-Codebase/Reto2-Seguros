# MongoDB local para la demo de Clara (sin autenticación).
# Uso: docker compose up -d
services:
  mongo:
    image: mongo:7
    container_name: clara-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
